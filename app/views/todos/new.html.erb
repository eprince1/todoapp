<h1>create new todo</h1>

<%= form_for @todo do |f| %>

<% if @todo.errors.any? %>

<h2>The form could not submmit because of the following:</h2>

<ul>
  <% @todo.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
</ul>

<% end %>

  <p>
    <%= f.label :name %> <br>
    <%= f.text_field :name %>
  </p>
  
  <p>
    <%= f.label :description %> <br>
    <%= f.text_area :description %>
  </p>
  
  <p>
    <%= f.submit %>
  </p>

<% end %>

<%= link_to "Your List of Todos", todos_path %>